# Archive (2026) transfer notes

This directory was copied from Google Drive for archival in the `UCST-AI` repository. Native Google Docs were exported to DOCX so the repository contains portable files.

Files at or above the conservative 100 MB GitHub standard upload threshold were excluded. See `ARCHIVE_TRANSFER_MANIFEST.json` for source mappings, methods, exclusions, and failures.
